<div class="post-body">
  <%= attachment_image_tag @post, :image %>
  <p>タイトル：<%= @post.title %></p>
  <p>テキスト：<%= @post.text %></p>
  <p>ユーザーネーム：<%= @post.user.name %></p>
  <p>投稿日時：<%= @post.created_at.strftime('%Y/%m/%d') %></p>
</div>