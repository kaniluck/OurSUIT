<table class='table'>
  <tr><%= attachment_image_tag(user, :profile_image, size: "100x100", format: 'jpeg', fallback: "no_image.jpg", class:"rounded-circle") %></tr>
  <tr>
	  <th>name</th>
	  <th><%= user.name %></th>
  </tr>
  <tr>
	  <th>intro</th>
	  <th><%= user.intro %></th>
  </tr>
  <tr>
    <th><%= link_to 'プロフィールを編集',edit_user_path(user),class: "btn btn-outline-secondary btn-block edit_user_#{user.id}" %></th>
  </tr>
  <tr>
    <th><%= link_to "ブックマーク", root_path %></th>
  </tr>
  <tr>
    <th><%= link_to "お気に入り", root_path %></th>
  </tr>
</table>